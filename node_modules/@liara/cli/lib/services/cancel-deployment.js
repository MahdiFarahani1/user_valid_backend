import retry from 'async-retry';
export default async (httpClient, releaseID, retryOptions) => {
    await retry(async () => {
        await httpClient.post(`v2/releases/${releaseID}/cancel`);
    }, retryOptions);
};
