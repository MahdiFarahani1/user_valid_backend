export default function spacing(maxLength, value) {
    const inputLength = value.toString().length;
    return inputLength === 1
        ? ' '.repeat(maxLength)
        : inputLength === maxLength
            ? ' '.repeat(1)
            : ' '.repeat(maxLength + 1 - inputLength);
}
